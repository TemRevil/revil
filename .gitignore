# Dependencies
node_modules/

# Build outputs
dist/
dist-ssr/
dev-dist/
build/
.next/
*.tsbuildinfo
.npm-cache/

# IDEs & Editor
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store

# Environment & Logs
.env
.env.local
.env.*.local
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Firebase
.firebase/
.firebaserc
# Track the Cloud Functions SOURCE (firebase.json points at it; it holds the whole
# backend + every audited security fix). Only its build output + deps are ignored —
# functions/node_modules is covered by the global node_modules/ rule above.
functions/lib/
*-firebase-adminsdk-*.json

# Project configuration
cors.json
.antigravity

# Local assistant / audit artifacts
.codex/
.claude/
.temp-claude-seo/
.hostinger-openapi.json
.hostinger-zone.json
.hostinger-zone-after.json
*.bak
AUDIT.md
AUDIT.ar.md

# Playwright MCP session artifacts
.playwright-mcp/

# Dev screenshots (root-level)
/*.png

# Internal docs (playbooks, setup notes — not for the public repo)
docs/
