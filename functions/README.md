# Firebase Functions

This directory contains the Cloud Functions for the Revil project.

## Important Note
The `syncMeeting` function is currently a placeholder. To fully enable Google Calendar synchronization, you must implement the Google Calendar API integration in `index.js`.

## Security
Do **NOT** commit `service-account.json` or any other secrets to this directory. Use Firebase Secrets or Application Default Credentials.
